import { create } from 'zustand';
import type { Edge, Node } from '@xyflow/react';
type Snapshot={nodes:Node[];edges:Edge[]};
type BuilderState={strategyId?:string;strategyName:string;nodes:Node[];edges:Edge[];past:Snapshot[];future:Snapshot[];clipboard:Node[];setGraph:(nodes:Node[],edges:Edge[])=>void;commit:(nodes:Node[],edges:Edge[])=>void;undo:()=>void;redo:()=>void;copy:(nodes:Node[])=>void;paste:()=>void;setStrategy:(id:string|undefined,name:string,nodes:Node[],edges:Edge[])=>void};
export const useBuilderStore=create<BuilderState>((set,get)=>({strategyName:'Untitled Strategy',nodes:[],edges:[],past:[],future:[],clipboard:[],setGraph:(nodes,edges)=>set({nodes,edges}),commit:(nodes,edges)=>set(s=>({past:[...s.past,{nodes:s.nodes,edges:s.edges}],future:[],nodes,edges})),undo:()=>set(s=>{const previous=s.past.at(-1);if(!previous)return s;return{...previous,past:s.past.slice(0,-1),future:[{nodes:s.nodes,edges:s.edges},...s.future]};}),redo:()=>set(s=>{const next=s.future[0];if(!next)return s;return{...next,past:[...s.past,{nodes:s.nodes,edges:s.edges}],future:s.future.slice(1)};}),copy:(nodes)=>set({clipboard:nodes}),paste:()=>{const s=get();const clones=s.clipboard.map(n=>({...n,id:crypto.randomUUID(),position:{x:n.position.x+32,y:n.position.y+32},selected:true}));set({nodes:[...s.nodes,...clones]});},setStrategy:(strategyId,strategyName,nodes,edges)=>set({strategyId,strategyName,nodes,edges,past:[],future:[]})}));
