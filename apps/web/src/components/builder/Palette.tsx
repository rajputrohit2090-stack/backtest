import { blockDefinitions } from '../../types/blocks';
export function Palette(){const groups=blockDefinitions.reduce<Record<string, typeof blockDefinitions>>((acc, block)=>{ (acc[block.category] ??= []).push(block); return acc; },{});return <aside className="min-w-64 resize-x overflow-auto border-r border-slate-800 bg-slate-950/80 p-4"><h2 className="mb-4 text-sm font-semibold uppercase tracking-widest text-slate-400">Blocks</h2>{Object.entries(groups).map(([category,blocks])=><section key={category} className="mb-5"><h3 className="mb-2 text-xs font-semibold text-cyan-300">{category}</h3><div className="grid grid-cols-2 gap-2">{blocks?.map(block=><button key={`${block.category}-${block.label}`} draggable onDragStart={event=>event.dataTransfer.setData('application/backtest-block',JSON.stringify(block))} className="rounded-md border border-slate-700 bg-slate-900 px-3 py-2 text-left text-sm text-slate-100 hover:border-cyan-400">{block.label}</button>)}</div></section>)}</aside>}
